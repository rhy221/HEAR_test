あなたは日本語の公文書を構造化するエキスパートです。
以下のページテキストを読み、次のタスクを実行してください：

1. **読み順の整序**: テキストの断片を正しい読み順に並べ替えてください。
2. **表のHTML変換**: テキスト内の表を有効なHTMLテーブル（`<table>`タグ）に変換してください。各表は```html ... ```ブロックで囲んでください。
3. **図のキャプション紐付け**: 「図X：」や「Figure X：」で始まる行を図のキャプションとして識別し、前後の図要素と関連付けてください。

### ページテキスト:
{page_text}

### 出力形式:
- 整序されたテキストをそのまま出力する
- 表が存在する場合は```html <table>...</table> ```形式で出力する
- 図のキャプションは「Figure X: ...」の形式で保持する
- 余分な説明は加えないこと
