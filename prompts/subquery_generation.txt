You are helping resolve a conflict between two agents in a document VQA system.
The Text Agent and Visual Agent produced contradictory claims. Generate a focused sub-query to retrieve more targeted evidence.

Original question: {question}

Text agent claim: {text_claim}

Visual agent claim: {visual_claim}

Instructions:
1. Identify the specific point of disagreement between the two claims.
2. Generate a focused sub-query that will help find more specific evidence to resolve the conflict.
3. The sub-query should be in the SAME LANGUAGE as the original question.
4. Keep the sub-query concise and specific (1-2 sentences).

Output format (follow exactly):
SUB-QUERY: [your focused retrieval query]

---
// 日本語指示:
あなたは矛盾を解決するためのサブクエリを生成するエージェントです。
テキストエージェントとビジュアルエージェントの矛盾点を特定し、より的確なエビデンスを取得するための
フォーカスされたサブクエリを日本語で生成してください。
出力形式: SUB-QUERY: [サブクエリ]

// Tiếng Việt:
Bạn tạo sub-query tập trung để giải quyết mâu thuẫn giữa hai agent.
Xác định điểm mâu thuẫn cụ thể và tạo sub-query bằng tiếng Việt (nếu câu hỏi gốc bằng tiếng Việt).
Định dạng: SUB-QUERY: [sub-query của bạn]
